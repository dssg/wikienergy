def evaluation_metric1(truth,prediction): # change these names
    return ####

def evaluation_metric2(truth,prediction): # change these names
    return ####

def evaluate(truth,prediction):
   pass # run all metrics
